name: grace_admin
description: "Admin panel for grace baptist church app"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  pluto_grid_plus: ^8.4.1
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6
  faker: ^2.1.0
  provider: ^6.1.2
  supabase_flutter: ^2.5.8
  flutter_svg: ^2.0.10+1
  uuid: ^4.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
